export enum ConversationStatus {
    None = 'notstarted',
    Listen = 'listening',
    Play = 'playing',
    Pause = 'paused',
    WaitUpload = 'waitingupload',
    Upload = 'uploading',
    Stop = 'stopped',
    Sending = 'sending',
  }
